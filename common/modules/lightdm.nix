{
  displayManager = {
    defaultSession = "none+bspwm";

    lightdm = {
      enable = true;

      # greeters.tiny = {
      #   enable = true;

      #   label = {
      #     user = "username";
      #     pass = "password";
      #   };

      #   extraConfig = ''''; # TODO: Make Tiny prettier
      # };
    };
  };
}
